# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# Windows shortcuts
*.url

# PowerShell
*.ps1~
*.psm1~

# Logs
*.log
logs/
*.log.*

# Local data and cache
data/
cache/
temp/
*.tmp

# Credentials and secrets
*.credentials
*.key
*.pem
*.pfx
secrets.json
.env
.env.*

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Build artifacts
bin/
obj/
*.dll
*.so
*.dylib

# Executables (too large for GitHub - deploy separately)
# gs-checker.exe lives at C:\start\ on each bay
*.exe

# Backup files
*.bak
*.backup
*.old

# Testing and debugging
test-results/
coverage/
*.test.log

# Local overrides (add this suffix to any file for local-only changes)
*.local.*
*-local.*

# Supervisor state files (these live outside the repo anyway)
supervisor.lock
restart-requested

# Bay-specific local configs (use bays.json instead)
config/local/
config/*.local.json

# License files (should not be committed - deploy separately)
*.lic
*.license

# Downloaded files and staging areas
files-from-bay/
course_staging/
dialog-image.jpg

# GSPro settings backups (these are machine-specific)
*.gss.orig
*.vgs.orig

# Copy/backup files
*kopia*
*- Copy/
*Copy (*
